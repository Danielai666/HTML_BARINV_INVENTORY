# Inventory Clicker (Static)

This repository hosts a **single-file** Inventory Clicker UI (`index.html`).

## Run locally
```bash
npm install
npm start
```
Then open the printed URL.

## Deploy to Railway
- Create a new Railway service from this GitHub repo.
- Railway will run: `npm start`
- The app will listen on `$PORT`.
